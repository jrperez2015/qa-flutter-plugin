---
description: Generate and run unit / widget / state tests for a Flutter feature using qa-flutter-unit-generator (no device required).
argument-hint: "<feature-name> [--auto] [--golden]"
---

Invoke the `qa-flutter:qa-flutter-unit-generator` skill with arguments: $ARGUMENTS

Follow the skill's SKILL.md exactly — semantic resolution, generation per layer (unit / widget / state), execution, coverage reporting.

If `--auto` is present, skip user prompts and accept top-scored candidates. If `--golden` is present, also generate widget golden tests for the feature's pages.
